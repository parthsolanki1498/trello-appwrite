/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains: ["cloud.appwrite.io","links.papareact.com"],
    },
    trailingSlash: true
};

module.exports = nextConfig
 